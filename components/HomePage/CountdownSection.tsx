import React from 'react';

interface Props {}

const CountdownSection: React.FC<Props> = (props) => {
  return (
      <div>
          CountdownSection
      </div>
  );
};

export default CountdownSection;